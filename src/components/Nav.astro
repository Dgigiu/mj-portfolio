---
const base = import.meta.env.BASE_URL.replace(/\/$/, "") + "/";
const path = Astro.url.pathname;
const norm = (p: string) => (p.endsWith("/") ? p : p + "/");

const isActive = (href: string) => {
  if (href === base) return norm(path) === norm(base);
  return path.startsWith(href);
};

const links = [
  { href: base, label: "Work" },
  { href: `${base}about/`, label: "About" },
  { href: `${base}contact/`, label: "Contact" },
];
---

<header class="nav">
  <div class="container nav-inner">
    <a href={base} class="brand" aria-label="Miguel Jesus, home">Miguel Jesus</a>
    <nav aria-label="Primary">
      <ul>
        {
          links.map((link) => (
            <li>
              <a
                href={link.href}
                class:list={["nav-link", { "is-active": isActive(link.href) }]}
                aria-current={isActive(link.href) ? "page" : undefined}
              >
                {link.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</header>

<style>
  .nav {
    padding: var(--space-6) 0;
    position: sticky;
    top: 0;
    background: var(--bg-canvas);
    z-index: 10;
    border-bottom: var(--hairline) solid var(--border-subtle);
  }

  .nav-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-5);
  }

  .brand {
    font-family: var(--font-sans);
    font-weight: var(--weight-semi);
    font-size: var(--text-lg);
    letter-spacing: var(--tracking-snug);
    text-decoration: none;
    color: var(--fg-primary);
  }

  nav ul {
    display: flex;
    gap: var(--space-5);
    list-style: none;
    margin: 0;
    padding: 0;
    flex-wrap: wrap;
    justify-content: flex-end;
  }

  .nav-link {
    position: relative;
    display: inline-block;
    font-family: var(--font-sans);
    font-size: var(--text-md);
    letter-spacing: var(--tracking-snug);
    color: var(--fg-secondary);
    text-decoration: none;
    padding-bottom: 2px;
    transition: color var(--duration-fast) var(--ease-out);
  }

  /* Underline as a pseudo-element (not border-bottom) so the hover state
     can grow in from the left instead of appearing. Sits where the old
     2px border did. */
  .nav-link::after {
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    height: var(--rule-bold);
    background: var(--fg-muted);
    scale: 0 1;
    transform-origin: 0 50%;
    transition: scale var(--duration-fast) var(--ease-out);
  }

  .nav-link:hover {
    color: var(--fg-primary);
  }

  .nav-link:hover::after {
    scale: 1;
  }

  .nav-link.is-active {
    color: var(--accent);
  }

  .nav-link.is-active::after {
    background: var(--accent);
    scale: 1;
  }

  @media (max-width: 480px) {
    nav ul {
      gap: var(--space-4);
    }
  }
</style>
